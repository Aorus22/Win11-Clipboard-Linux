[package]
name = "win11-clipboard-core"
version = "0.8.0"
description = "Core backend library for Windows 11 Clipboard History For Linux"
authors = ["gustavosett"]
license = "MIT"
repository = "https://github.com/gustavosett/Windows-11-Clipboard-Linux"
edition = "2021"
rust-version = "1.77"

[lib]
name = "win11_clipboard_core"
crate-type = ["rlib"]

[dependencies]
# Clipboard Management - wayland-data-control for native Wayland support
arboard = { version = "3.4", features = ["image-data", "wayland-data-control"] }

# Async Runtime
tokio = { version = "1.52.2", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Image Handling
image = "0.25.10"
base64 = "0.23"

# HTTP Client for GIF downloads
reqwest = { version = "0.13.3", features = ["blocking"] }

# Utilities
once_cell = "1.21.4"
parking_lot = "0.12"
chrono = { version = "0.4.44", features = ["serde"] }
uuid = { version = "1.23.1", features = ["v4", "v5", "serde"] }
dirs = "6.0"

# System information
whoami = "2.1.2"

# URL encoding for LXQt shortcuts
percent-encoding = "2.3"

# X11 Simulation for paste injection
x11rb = { version = "0.14", features = ["allow-unsafe-code", "xtest"] }
libc = "0.2"

# D-Bus for XDG Desktop Portal (theme detection for COSMIC and other DEs)
zbus = { version = "5.15.0", default-features = false, features = ["tokio"] }
futures-lite = "2"
