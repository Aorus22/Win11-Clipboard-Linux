//! GPUI views — pixel-parity port of the React components.
//!
//! Token source: `tailwind.config.js` + `index.css`. Base font 14px, so
//! `text-sm` = 12.25px and `text-xs` = 10.5px (set explicitly via `text_size`).

pub mod header;
pub mod history_item;
pub mod icons;
pub mod pickers;
pub mod popup;
pub mod search;
pub mod tabbar;
